#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
<<<<<<< HEAD
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@typescript-eslint/eslint-plugin/node_modules/semver/bin/semver.js" "$@"
  ret=$?
else 
  node  "$basedir/../@typescript-eslint/eslint-plugin/node_modules/semver/bin/semver.js" "$@"
  ret=$?
fi
exit $ret
=======
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../semver/bin/semver.js" "$@"
else 
  exec node  "$basedir/../semver/bin/semver.js" "$@"
fi
>>>>>>> 7ae4ad4a182b28aa5d9d73ad9f3d040d6bc884b0
